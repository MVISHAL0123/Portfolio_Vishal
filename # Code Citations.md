# Code Citations

## License: unknown
https://github.com/the2dge/learning/blob/92b2de692ed304799b92645cd88d4072f8b6a984/GAScode.js

```
✅ Done! Your code is now connected to the new Google Apps Script URL.

**Test the form now** - your dev server should auto-refresh, and you should be able to send messages! 

If you still get a CORS error, it means the `doOptions` function wasn't added to your Google Apps Script. Make sure your script looks like this:

```javascript
function doPost(e) {
  const params = JSON.parse(e.postData.contents);
  const name = params.name || "No name";
  const email = params.email || "No email";
  const message = params.message || "No message";

  try {
    MailApp.sendEmail(
      'vishalm102005@gmail.com',
      `New Contact Form Message from ${name}`,
      `From: ${email}\n\nMessage:\n${message}`
    );
    return ContentService.createTextOutput(JSON.stringify({ success: true }))
      .setMimeType(ContentService.MimeType.JSON);
  } catch (error) {
    return ContentService.createTextOutput(JSON.stringify({ success: false, error: error.toString() }))
      .setMimeType(ContentService.MimeType.JSON);
  }
```


## License: unknown
https://github.com/the2dge/learning/blob/92b2de692ed304799b92645cd88d4072f8b6a984/GAScode.js

```
✅ Done! Your code is now connected to the new Google Apps Script URL.

**Test the form now** - your dev server should auto-refresh, and you should be able to send messages! 

If you still get a CORS error, it means the `doOptions` function wasn't added to your Google Apps Script. Make sure your script looks like this:

```javascript
function doPost(e) {
  const params = JSON.parse(e.postData.contents);
  const name = params.name || "No name";
  const email = params.email || "No email";
  const message = params.message || "No message";

  try {
    MailApp.sendEmail(
      'vishalm102005@gmail.com',
      `New Contact Form Message from ${name}`,
      `From: ${email}\n\nMessage:\n${message}`
    );
    return ContentService.createTextOutput(JSON.stringify({ success: true }))
      .setMimeType(ContentService.MimeType.JSON);
  } catch (error) {
    return ContentService.createTextOutput(JSON.stringify({ success: false, error: error.toString() }))
      .setMimeType(ContentService.MimeType.JSON);
  }
```


## License: unknown
https://github.com/the2dge/learning/blob/92b2de692ed304799b92645cd88d4072f8b6a984/GAScode.js

```
✅ Done! Your code is now connected to the new Google Apps Script URL.

**Test the form now** - your dev server should auto-refresh, and you should be able to send messages! 

If you still get a CORS error, it means the `doOptions` function wasn't added to your Google Apps Script. Make sure your script looks like this:

```javascript
function doPost(e) {
  const params = JSON.parse(e.postData.contents);
  const name = params.name || "No name";
  const email = params.email || "No email";
  const message = params.message || "No message";

  try {
    MailApp.sendEmail(
      'vishalm102005@gmail.com',
      `New Contact Form Message from ${name}`,
      `From: ${email}\n\nMessage:\n${message}`
    );
    return ContentService.createTextOutput(JSON.stringify({ success: true }))
      .setMimeType(ContentService.MimeType.JSON);
  } catch (error) {
    return ContentService.createTextOutput(JSON.stringify({ success: false, error: error.toString() }))
      .setMimeType(ContentService.MimeType.JSON);
  }
```


## License: unknown
https://github.com/the2dge/learning/blob/92b2de692ed304799b92645cd88d4072f8b6a984/GAScode.js

```
✅ Done! Your code is now connected to the new Google Apps Script URL.

**Test the form now** - your dev server should auto-refresh, and you should be able to send messages! 

If you still get a CORS error, it means the `doOptions` function wasn't added to your Google Apps Script. Make sure your script looks like this:

```javascript
function doPost(e) {
  const params = JSON.parse(e.postData.contents);
  const name = params.name || "No name";
  const email = params.email || "No email";
  const message = params.message || "No message";

  try {
    MailApp.sendEmail(
      'vishalm102005@gmail.com',
      `New Contact Form Message from ${name}`,
      `From: ${email}\n\nMessage:\n${message}`
    );
    return ContentService.createTextOutput(JSON.stringify({ success: true }))
      .setMimeType(ContentService.MimeType.JSON);
  } catch (error) {
    return ContentService.createTextOutput(JSON.stringify({ success: false, error: error.toString() }))
      .setMimeType(ContentService.MimeType.JSON);
  }
```


## License: unknown
https://github.com/the2dge/learning/blob/92b2de692ed304799b92645cd88d4072f8b6a984/GAScode.js

```
✅ Done! Your code is now connected to the new Google Apps Script URL.

**Test the form now** - your dev server should auto-refresh, and you should be able to send messages! 

If you still get a CORS error, it means the `doOptions` function wasn't added to your Google Apps Script. Make sure your script looks like this:

```javascript
function doPost(e) {
  const params = JSON.parse(e.postData.contents);
  const name = params.name || "No name";
  const email = params.email || "No email";
  const message = params.message || "No message";

  try {
    MailApp.sendEmail(
      'vishalm102005@gmail.com',
      `New Contact Form Message from ${name}`,
      `From: ${email}\n\nMessage:\n${message}`
    );
    return ContentService.createTextOutput(JSON.stringify({ success: true }))
      .setMimeType(ContentService.MimeType.JSON);
  } catch (error) {
    return ContentService.createTextOutput(JSON.stringify({ success: false, error: error.toString() }))
      .setMimeType(ContentService.MimeType.JSON);
  }
```


## License: unknown
https://github.com/the2dge/learning/blob/92b2de692ed304799b92645cd88d4072f8b6a984/GAScode.js

```
✅ Done! Your code is now connected to the new Google Apps Script URL.

**Test the form now** - your dev server should auto-refresh, and you should be able to send messages! 

If you still get a CORS error, it means the `doOptions` function wasn't added to your Google Apps Script. Make sure your script looks like this:

```javascript
function doPost(e) {
  const params = JSON.parse(e.postData.contents);
  const name = params.name || "No name";
  const email = params.email || "No email";
  const message = params.message || "No message";

  try {
    MailApp.sendEmail(
      'vishalm102005@gmail.com',
      `New Contact Form Message from ${name}`,
      `From: ${email}\n\nMessage:\n${message}`
    );
    return ContentService.createTextOutput(JSON.stringify({ success: true }))
      .setMimeType(ContentService.MimeType.JSON);
  } catch (error) {
    return ContentService.createTextOutput(JSON.stringify({ success: false, error: error.toString() }))
      .setMimeType(ContentService.MimeType.JSON);
  }
}

function doOptions(e) {
  return ContentService.createTextOutput()
    .setHeader
```

